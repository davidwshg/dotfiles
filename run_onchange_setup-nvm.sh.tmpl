#!/bin/bash
set -euo pipefail

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] || { echo "nvm not found, skipping"; exit 0; }
. "$NVM_DIR/nvm.sh"

nvm install --lts
nvm alias default lts/*
echo "Node $(node --version) set as default via nvm"
